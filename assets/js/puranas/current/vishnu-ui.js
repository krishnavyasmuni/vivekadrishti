(() => {
  // Compatibility shim only. Vishnu Purana now uses the single shared Mahapurana
  // interface in mahapuranas-ui.js. Keeping a second disclosure handler here caused
  // each click to toggle twice, so the old Vishnu-only UI is intentionally disabled.
})();
